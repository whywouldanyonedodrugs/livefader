# requirements.in
aiohttp                 # async HTTP client
asyncpg                 # Postgres driver
ccxt                    # Bybit & other exchanges
numpy<2                 # Arrow & SciPy ecosystem still expect 1.x
pandas                  # DataFrames
pandas_ta               # pure‑Python TA indicators (fallback when TA‑Lib absent)
pyarrow<16              # PyArrow 15.x caps NumPy<2
pydantic<2.0            # v1.x because BaseSettings lives here
python-dotenv           # loads .env for pydantic
PyYAML                  # YAML loader
tqdm                    # progress bars
watchdog                # hot‑reload file watcher
